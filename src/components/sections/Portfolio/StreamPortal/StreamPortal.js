import React from 'react';
import PictureViewer from '../../../common/PictureViewer';


const images = [];

const StreamPortal = () => <PictureViewer images={images} />;

export default StreamPortal;
