import React from 'react';
import PictureViewer from '../../../common/PictureViewer';
import images from './images';


const Flexer = () => {

    return (
        <div>
            Some description about Flexer
        </div>
    );
};

export default Flexer;
